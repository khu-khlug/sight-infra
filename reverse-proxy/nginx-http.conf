server {
  listen 80;
  server_name _;

  # ACME 챌린지
  location /.well-known/acme-challenge/ {
    root /var/www/certbot;
  }

  # 그 외는 HTTPS로 리다이렉트
  location / {
    return 301 https://$host$request_uri;
  }
}
